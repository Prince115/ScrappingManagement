@model ScrappingManagement.Web.Dto.CustomerLedgerDtos

@{
    ViewData["Title"] = "Customer Ledger";
    var Customers = ViewBag.Customers as List<ScrappingManagement.Web.Models.Customer>;
}

<h2>Customer Ledger</h2>

<form asp-action="Index" method="get" class="mb-4">
    <div class="row">
        <div class="col-md-4 mb-3">
            <label for="selectedCustomerId" class="form-label">Customer:</label>
            <select name="selectedCustomerId" id="selectedCustomerId" class="form-select">
                <option value="">-- Select Customer --</option>
                @if (Customers != null)
                {
                    foreach (var Customer in Customers)
                    {
                        <option value="@Customer.Id" selected="@(Model.SelectedCustomerId == Customer.Id)">@Customer.Name</option>
                    }
                }
            </select>
        </div>
        <div class="col-md-3 mb-3">
            <label for="fromDate" class="form-label">From Date:</label>
            <input type="date" name="fromDate" id="fromDate" class="form-control" value="@(Model.FromDate.HasValue? Model.FromDate.Value.ToString("yyyy-MM-dd") : "")" />
        </div>
        <div class="col-md-3 mb-3">
            <label for="toDate" class="form-label">To Date:</label>
            <input type="date" name="toDate" id="toDate" class="form-control" value="@(Model.ToDate.HasValue? Model.ToDate.Value.ToString("yyyy-MM-dd") : "")" />
        </div>
        <div class="col-md-2 mb-3 d-flex align-items-end">
            <button type="submit" class="btn btn-primary me-2">Filter</button>
            <a asp-action="Index" class="btn btn-secondary me-2">Clear</a>
            <button type="button" class="btn btn-info" id="printLedgerBtn" @(!Model.SelectedCustomerId.HasValue ? "disabled" : "")><i class="bi bi-printer"></i> Print</button>
        </div>
    </div>
</form>

@if (Model.SelectedCustomerId.HasValue)
{
    <div id="ledgerContent">
        <div class="alert alert-info mt-3">
            <strong>Total Debit:</strong> @Model.TotalDebit.ToString("N2") |
            <strong>Total Credit:</strong> @Model.TotalCredit.ToString("N2") |
            <strong>Due Amount:</strong>
            <span class="@(Model.DueAmount > 0 ? "text-danger" : "text-success") fw-bold">
                @Model.DueAmount.ToString("N2")
            </span>
        </div>

        <div class="row mt-4">
            <div class="col-md-6">
                <h6>Debits (Invoices)</h6>
                <table class="table table-sm table-bordered">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>Bill No.</th>
                            <th>Amount</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var entry in Model.LedgerEntries.Where(e => e.Type == "Invoice").OrderBy(e => e.Date))
                        {
                            <tr>
                                <td>@entry.Date.ToString("dd-MM-yyyy")</td>
                                <td>
                                    <a asp-controller="Invoices" asp-action="Details" asp-route-id="@entry.DocumentId">
                                        @entry.Description
                                    </a>
                                </td>
                                <td class="text-end">@entry.Debit.ToString("N2")</td>
                            </tr>
                        }
                        @if (!Model.LedgerEntries.Any(e => e.Type == "Invoice"))
                        {
                            <tr><td colspan="3">No Invoices found for this Customer and date range.</td></tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="2" class="text-end fw-bold">Total Debits</td>
                            <td class="text-end fw-bold">@Model.TotalDebit.ToString("N2")</td>
                        </tr>
                    </tfoot>
                </table>
            </div>
            <div class="col-md-6">
                <h6>Credits (Receipts)</h6>
                <table class="table table-sm table-bordered">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>Description</th>
                            <th>Amount</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var entry in Model.LedgerEntries.Where(e => e.Type == "Receipt").OrderBy(e => e.Date))
                        {
                            <tr>
                                <td>@entry.Date.ToString("dd-MM-yyyy")</td>
                                <td>
                                    <a asp-controller="Receipts" asp-action="Details" asp-route-id="@entry.DocumentId">
                                        @entry.Description
                                    </a>
                                </td>
                                <td class="text-end">@entry.Credit.ToString("N2")</td>
                            </tr>
                        }
                        @if (!Model.LedgerEntries.Any(e => e.Type == "Receipt"))
                        {
                            <tr><td colspan="3">No Receipts found for this Customer and date range.</td></tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="2" class="text-end fw-bold">Total Credits</td>
                            <td class="text-end fw-bold">@Model.TotalCredit.ToString("N2")</td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
}
else
{
    <p>Please select a Customer to view their ledger.</p>
}

@section Scripts {
   <script src="~/js/Customer-ledger-print.js" asp-append-version="true"></script>
}
