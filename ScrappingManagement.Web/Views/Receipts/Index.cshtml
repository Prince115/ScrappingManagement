@model ReceiptIndexViewModel
@using ScrappingManagement.Web.Models
@using Microsoft.AspNetCore.Mvc.Rendering
@using ScrappingManagement.Web.Dto

@{
    ViewData["Title"] = "Receipts";
    var prevDisabled = !Model.Receipts.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.Receipts.HasNextPage ? "disabled" : "";

    var currentPageSize = Model.CurrentPageSize;
    var currentCustomerFilter = Model.CurrentCustomerFilter;
    var currentPaymentModeFilter = Model.CurrentPaymentModeFilter;
    var currentFromDateFilter = Model.CurrentFromDateFilter;
    var currentToDateFilter = Model.CurrentToDateFilter;
}

<link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
<link href="https://cdn.jsdelivr.net/npm/select2-bootstrap-5-theme@1.3.0/dist/select2-bootstrap-5-theme.min.css" rel="stylesheet" />

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Receipts</h2>
    <p class="mb-0">
        <a asp-action="Create" class="btn btn-primary">Create New</a>
    </p>
</div>

<form asp-action="Index" method="get" class="mb-3">
    <div class="row">
        <div class="col-md-3">
            <div class="form-group">
                <label for="supplierId" class="control-label">Customer</label>
                <select name="supplierId" id="supplierId" class="form-control"
                        asp-items='@(new SelectList(Model.Customers, "Id", "Name", currentCustomerFilter))'>
                    <option value="">All Suppliers</option>
                </select>
            </div>
        </div>
        <div class="col-md-3">
            <div class="form-group">
                <label for="PaymentMode" class="control-label">Payment Mode</label>
                <select name="PaymentMode" id="PaymentMode" class="form-control" asp-items="Html.GetEnumSelectList<PaymentMode>()">
                    <option value="">All Modes</option>
                </select>
            </div>
        </div>
        <div class="col-md-2">
            <div class="form-group">
                <label for="fromDate" class="control-label">From Date</label>
                <input type="date" name="fromDate" id="fromDate" class="form-control" value="@currentFromDateFilter" />
            </div>
        </div>
        <div class="col-md-2">
            <div class="form-group">
                <label for="toDate" class="control-label">To Date</label>
                <input type="date" name="toDate" id="toDate" class="form-control" value="@currentToDateFilter" />
            </div>
        </div>
        <div class="col-md-2 text-center align-self-end">
            <button type="submit" class="btn btn-primary">Filter</button>
            <a asp-action="Index" class="btn btn-secondary">Clear</a>
        </div>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Receipts.First().Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Receipts.First().Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Receipts.First().CustomerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Receipts.First().PaymentMode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Receipts.First().Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Receipts.First().Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Receipts)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @item.Date.ToString("dd-MMMM-yyyy")
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CustomerName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PaymentMode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Amount)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<div class="d-flex justify-content-center">
    <nav aria-label="Quotes pagination">
        <ul class="pagination">
            <li class="page-item @prevDisabled">
                <a asp-action="Index"
                   asp-route-pageNumber="@(Model.Receipts.PageIndex - 1)"
                   asp-route-supplierId="@currentCustomerFilter"
                   asp-route-fromDate="@currentFromDateFilter"
                   asp-route-toDate="@currentToDateFilter"
                   asp-route-pageSize="@currentPageSize"
                   class="page-link">Previous</a>
            </li>

            @for (int i = 1; i <= Model.Receipts.TotalPages; i++)
            {
                <li class="page-item @(i == Model.Receipts.PageIndex ? "active" : "")">
                    <a asp-action="Index"
                       asp-route-pageNumber="@i"
                       asp-route-supplierId="@currentCustomerFilter"
                       asp-route-fromDate="@currentFromDateFilter"
                       asp-route-toDate="@currentToDateFilter"
                       asp-route-pageSize="@currentPageSize"
                       class="page-link">@i</a>
                </li>
            }

            <li class="page-item @nextDisabled">
                <a asp-action="Index"
                   asp-route-pageNumber="@(Model.Receipts.PageIndex + 1)"
                   asp-route-supplierId="@currentCustomerFilter"
                   asp-route-fromDate="@currentFromDateFilter"
                   asp-route-toDate="@currentToDateFilter"
                   asp-route-pageSize="@currentPageSize"
                   class="page-link">Next</a>
            </li>
            <li class="page-item d-flex align-items-center ms-3">
                <label for="pageSize" class="form-label mb-0 me-2">Show:</label>
                <select name="pageSize" id="pageSize" class="form-select form-select-sm auto-submit" style="width: auto;">
                    <option value="5" selected="@(Model.CurrentPageSize.ToString() == "5")">5</option>
                    <option value="10" selected="@(Model.CurrentPageSize.ToString() == "10")">10</option>
                    <option value="20" selected="@(Model.CurrentPageSize.ToString() == "20")">20</option>
                    <option value="50" selected="@(Model.CurrentPageSize.ToString() == "50")">50</option>
                </select>
            </li>
        </ul>
    </nav>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        $(document).ready(function () {
            $('#supplierId').select2({
                theme: 'bootstrap-5',
                placeholder: 'Select a Customer',
                allowClear: true,
                width: 'resolve'
            });

            $('#PaymentMode').select2({
                theme: 'bootstrap-5',
                placeholder: 'Select a Payment Mode',
                allowClear: true,
                width: 'resolve'
            });

            // Set initial values for Select2 dropdowns if filters are present
            var currentSupplier = '@currentCustomerFilter';
            if (currentSupplier) {
                $('#supplierId').val(currentSupplier).trigger('change');
            }

            var currentPaymentMode = '@((int?)currentPaymentModeFilter)';
            if (currentPaymentMode) {
                $('#PaymentMode').val(currentPaymentMode).trigger('change');
            }

            // Handle page size change
            $('#pageSize').change(function () {
                $(this).closest('form').submit();
            });
        });
    </script>
}
